// Script JS nếu cần xử lý sự kiện phía client

document.addEventListener("DOMContentLoaded", function() {
    const form = document.querySelector("form");

    // Thêm các sự kiện nếu cần thiết trong tương lai
});
